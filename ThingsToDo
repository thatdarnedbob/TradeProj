# Ever updating list of the very next things to do

Town knows:

- Its name
- Its # of pop units
- Its production

Ship knows:

- Its name
- Its location (current port)
- Its cargo
- Its activity

World Manager knows:

- Each town's name, tied to a number (dict?)
- distance matrix for towns

World Manager does:

- Generate Goods
- Generate distance matrices

Let's revise:

TradeModel

- Initializes view
- Sets up WorldManager
- Creates inital Main Menu & the 'pointer location' stack
- For each line in Main Menu, asks WorldManager to provide a list of menu items when that line is selected
- Basically is a menu handler
